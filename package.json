{
  "name": "duckdb-notebook",
  "displayName": "DuckDB Notebook",
  "description": "Interactive DuckDB SQL notebooks with language server support",
  "version": "0.1.0",
  "publisher": "duckdb-notebook",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["Notebooks", "Programming Languages"],
  "keywords": ["duckdb", "sql", "notebook", "database"],
  "activationEvents": [
    "onNotebook:duckdb-notebook",
    "onLanguage:duckdb-sql"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "notebooks": [
      {
        "type": "duckdb-notebook",
        "displayName": "DuckDB Notebook",
        "selector": [
          { "filenamePattern": "*.duckdb-notebook" },
          { "filenamePattern": "*.ddb" }
        ]
      }
    ],
    "languages": [
      {
        "id": "duckdb-sql",
        "aliases": ["DuckDB SQL", "duckdb"],
        "extensions": [".duckdb-sql"],
        "configuration": "./language-configuration.json"
      }
    ],
    "commands": [
      {
        "command": "duckdb-notebook.new",
        "title": "New DuckDB Notebook",
        "category": "DuckDB"
      }
    ],
    "configuration": {
      "title": "DuckDB Notebook",
      "properties": {
        "duckdb-notebook.maxRows": {
          "type": "number",
          "default": 1000,
          "description": "Maximum number of rows to display in query results"
        },
        "duckdb-notebook.databasePath": {
          "type": "string",
          "default": ":memory:",
          "description": "Path to DuckDB database file (use :memory: for in-memory)"
        },
        "duckdb-notebook.languageServer.trace": {
          "type": "string",
          "enum": ["off", "messages", "verbose"],
          "default": "off",
          "description": "Traces the communication between VS Code and the language server"
        }
      }
    }
  },
  "scripts": {
    "compile": "node esbuild.js",
    "watch": "node esbuild.js --watch",
    "package": "vsce package",
    "lint": "eslint src/ server/ --ext ts"
  },
  "dependencies": {
    "@duckdb/node-api": "^1.4.0-r.1",
    "vscode-languageclient": "^9.0.1",
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.11"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
